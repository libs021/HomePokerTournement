<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.libbys.homepokertournement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PlayerListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomeActivity"
            android:label="HomeActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TournamentActivity"
            android:label="@string/NewTournament"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TournamentPreview"
            android:label="@string/tournamentPreview"
            android:theme="@style/AppTheme" />
        <provider
            android:name=".DataBaseFiles.PokerProvider"
            android:authorities="com.example.libbys.homepokertournement"
            android:exported="false" />
    </application>

</manifest>